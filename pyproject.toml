[tool.pytest.ini_options]
markers = [
    "integration: touches a real network service; skips itself if unreachable",
    "agentic: touches the real Groq API; skips itself unless GROQ_API_KEY is set",
]